apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wealist-dev

resources:
  # Shared resources (ConfigMap, Secret for all services)
  - ../../../../../k8s/base/shared
  # Service-specific resources
  - ../../base

images:
  - name: chat-service
    newTag: local

replicas:
  - name: chat-service
    count: 1

patches:
  - target:
      kind: ConfigMap
      name: chat-service-config
    patch: |-
      - op: replace
        path: /data/GIN_MODE
        value: "debug"
