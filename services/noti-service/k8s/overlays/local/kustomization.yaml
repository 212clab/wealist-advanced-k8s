apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wealist-dev

resources:
  - ../../base

images:
  - name: noti-service
    newName: 212clab/wealist-noti-service
    newTag: v1

replicas:
  - name: noti-service
    count: 1

patches:
  - target:
      kind: ConfigMap
      name: noti-service-config
    patch: |-
      - op: replace
        path: /data/SPRING_PROFILES_ACTIVE
        value: "dev"
